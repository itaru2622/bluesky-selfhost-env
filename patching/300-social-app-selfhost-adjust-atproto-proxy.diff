diff --git a/Dockerfile b/Dockerfile
index 93b17a86b..ed9bfad7d 100644
--- a/Dockerfile
+++ b/Dockerfile
@@ -33,6 +33,8 @@ ENV EXPO_PUBLIC_RELEASE_VERSION=$EXPO_PUBLIC_RELEASE_VERSION
 ARG EXPO_PUBLIC_BUNDLE_IDENTIFIER
 # If not set by GitHub workflows, we're probably in Render
 ENV EXPO_PUBLIC_BUNDLE_IDENTIFIER=${EXPO_PUBLIC_BUNDLE_IDENTIFIER:-$RENDER_GIT_COMMIT}
+ARG EXPO_PUBLIC_BLUESKY_PROXY_DID=did:web:api.bsky.app
+ENV EXPO_PUBLIC_BLUESKY_PROXY_DID=$EXPO_PUBLIC_BLUESKY_PROXY_DID
 
 #
 # Sentry
@@ -64,7 +66,11 @@ RUN \. "$NVM_DIR/nvm.sh" && \
   echo "EXPO_PUBLIC_RELEASE_VERSION=$EXPO_PUBLIC_RELEASE_VERSION" >> .env && \
   echo "EXPO_PUBLIC_BUNDLE_IDENTIFIER=$EXPO_PUBLIC_BUNDLE_IDENTIFIER" >> .env && \
   echo "EXPO_PUBLIC_BUNDLE_DATE=$(date -u +"%y%m%d%H")" >> .env && \
+  echo "EXPO_PUBLIC_BLUESKY_PROXY_DID=$EXPO_PUBLIC_BLUESKY_PROXY_DID" >> .env && \
   echo "EXPO_PUBLIC_SENTRY_DSN=$EXPO_PUBLIC_SENTRY_DSN" >> .env && \
+  echo "##########################" && \
+  cat .env && \
+  echo "##########################" && \
   npm install --global yarn && \
   yarn && \
   yarn intl:build 2>&1 | tee i18n.log && \
@@ -89,7 +95,7 @@ RUN cd bskyweb/ && \
     -o /bskyweb \
     ./cmd/bskyweb
 
-FROM debian:bullseye-slim
+#FROM debian:bullseye-slim
 
 ENV GODEBUG=netdns=go
 ENV TZ=Etc/UTC
@@ -101,8 +107,9 @@ RUN apt-get update && apt-get install --yes \
 
 ENTRYPOINT ["dumb-init", "--"]
 
-WORKDIR /bskyweb
-COPY --from=build-env /bskyweb /usr/bin/bskyweb
+#WORKDIR /bskyweb
+#COPY --from=build-env /bskyweb /usr/bin/bskyweb
+RUN cp -p /bskyweb /usr/bin/bskyweb
 
 CMD ["/usr/bin/bskyweb"]
 
